# ============================================
# Variables de Entorno - Backend
# ============================================
# Copie este archivo como .env y ajuste los valores según su entorno

# ==========================================
# Base de datos
# ==========================================
# URL de conexión a PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
#
# Desarrollo local:
DATABASE_URL=postgresql://tienda_user:supersecretpassword123@localhost:5432/mi_tienda_db?schema=public
#
# Docker (desde dentro del contenedor):
# DATABASE_URL=postgresql://tienda_user:supersecretpassword123@postgres:5432/mi_tienda_db?schema=public
#
# Producción:
# DATABASE_URL=postgresql://user:password@production-host:5432/database?schema=public

# ==========================================
# Servidor
# ==========================================
# Puerto en el que se ejecutará el servidor
PORT=8080

# Entorno de ejecución (development | production | test)
NODE_ENV=development

# ==========================================
# Autenticación JWT
# ==========================================
# Clave secreta para firmar tokens JWT
# IMPORTANTE: En producción, use una clave aleatoria fuerte de al menos 32 caracteres
# Puede generar una con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=mi-clave-secreta-para-jwt-2025

# ==========================================
# CORS (Cross-Origin Resource Sharing)
# ==========================================
# Lista de orígenes permitidos separados por comas
# Ejemplos:
#   - Desarrollo: http://localhost:3000
#   - Producción: https://www.example.com,https://app.example.com
#   - Permitir todos (NO recomendado en producción): *
CORS_ORIGINS=http://localhost:3000

# ==========================================
# Notas de Configuración
# ==========================================
#
# DESARROLLO LOCAL (sin Docker):
# - DATABASE_URL=postgresql://tienda_user:password@localhost:5432/mi_tienda_db?schema=public
# - PORT=8080
# - NODE_ENV=development
# - CORS_ORIGINS=http://localhost:3000
#
# DOCKER (desarrollo):
# - DATABASE_URL=postgresql://tienda_user:password@postgres:5432/mi_tienda_db?schema=public
# - PORT=8080
# - NODE_ENV=production
# - CORS_ORIGINS=http://localhost:3000
#
# PRODUCCIÓN:
# - DATABASE_URL=<URL de base de datos en producción>
# - PORT=8080
# - NODE_ENV=production
# - CORS_ORIGINS=https://www.your-domain.com,https://your-domain.com
# - JWT_SECRET=<genere una clave aleatoria fuerte>
