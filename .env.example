# ============================================
# Variables de Entorno para Docker Compose
# ============================================
# Copie este archivo como .env y ajuste los valores según su entorno

# ==========================================
# Base de datos PostgreSQL
# ==========================================
POSTGRES_USER=tienda_user
POSTGRES_PASSWORD=supersecretpassword123
POSTGRES_DB=mi_tienda_db

# ==========================================
# Backend (Express API)
# ==========================================
PORT=8080
JWT_SECRET=mi-clave-secreta-para-jwt-2025
NODE_ENV=production

# CORS: Lista de orígenes permitidos separados por comas
# En producción, reemplace con sus dominios reales:
# CORS_ORIGINS=https://www.example.com,https://api.example.com
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# ==========================================
# Frontend (Next.js)
# ==========================================

# URL del API para el navegador (cliente)
# En producción, use su FQDN real:
# NEXT_PUBLIC_API_URL=https://api.your-domain.com/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# URL base del backend para construcción de URLs de imágenes en el navegador
# En producción, use su FQDN real:
# NEXT_PUBLIC_API_BASE_URL=https://api.your-domain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# URL interna del backend para comunicación servidor-a-servidor (dentro de Docker)
# En Docker/K8s, use el nombre del servicio:
NEXT_INTERNAL_API_BASE_URL=http://backend:8080

# ==========================================
# Notas de Configuración
# ==========================================
#
# DESARROLLO LOCAL (sin Docker):
# - NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
# - NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# - CORS_ORIGINS=http://localhost:3000
#
# DOCKER (desarrollo local):
# - NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
# - NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# - NEXT_INTERNAL_API_BASE_URL=http://backend:8080
# - CORS_ORIGINS=http://localhost:3000
#
# PRODUCCIÓN (con dominio):
# - NEXT_PUBLIC_API_URL=https://api.your-domain.com/api/v1
# - NEXT_PUBLIC_API_BASE_URL=https://api.your-domain.com
# - NEXT_INTERNAL_API_BASE_URL=http://backend:8080
# - CORS_ORIGINS=https://www.your-domain.com,https://your-domain.com
# - JWT_SECRET=<use un valor aleatorio fuerte>
# - POSTGRES_PASSWORD=<use una contraseña fuerte>
